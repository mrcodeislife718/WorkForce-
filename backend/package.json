{
  "name": "orca-store-backend",
  "version": "1.1.0",
  "description": "ORCA universal distribution and management platform for digital employees",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate:universal": "node scripts/migrateUniversal.js",
    "seed": "node scripts/seedUniversal.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check migrations/legacyUniversalMigration.js && node --check services/deploymentService.js && node --check routes/telemetry.js"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0",
    "sequelize": "^6.37.8",
    "sequelize-cli": "^6.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
